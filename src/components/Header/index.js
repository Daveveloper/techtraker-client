import React from 'react';
import './css/header.css';

const Header = () => (
    <header className='AppHeader'>
        <h1>TECH TRACKER</h1>
        <p>List of technoligies most used in Accenture Costa Rica, click the "add button +" if in your team are using some of this technologies.</p>
    </header>
);

export default Header;
